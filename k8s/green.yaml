apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: engine
      version: green
  template:
    metadata:
      labels:
        app: engine
        version: green
    spec:
      containers:
      - name: engine
        image: trading-engine:latest
        imagePullPolicy: Never
